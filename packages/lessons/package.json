{
  "name": "lessons",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx build.ts",
    "dev": "yarn build && chokidar '**' -c 'yarn build'"
  },
  "devDependencies": {
    "@webcontainer/api": "^1.0.2",
    "chokidar-cli": "^3.0.0",
    "tsx": "^3.12.5"
  },
  "volta": {
    "extends": "../../package.json"
  },
  "workspaces": [
    "*"
  ]
}
