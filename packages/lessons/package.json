{
  "name": "lessons",
  "type": "module",
  "private": true,
  "workspaces": [
    "*"
  ],
  "scripts": {
    "build": "tsx build.ts",
    "dev": "yarn build && chokidar '**' -c 'yarn build'"
  },
  "devDependencies": {
    "@types/node": "^22.7.6",
    "@webcontainer/api": "^1.4.0",
    "chokidar-cli": "^3.0.0",
    "tsx": "^4.19.1"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
